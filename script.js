class mycalss {
    constructor(name="tmp") {
      this.name = name;
    }
    greet() {
      //console.log(`Hello, ${this.name}!`);
      alert(origin);
    }
  }
  // Expose it globally
  window.mycalss = myclass;
  